[package]
name        = "tatara-rust-proptest"
description = "Property-based testing primitives — random `Ident`/`TypeRef`/Spec generators + invariant assertions (emit+re-parse identity, validate stability, no-panic on arbitrary input). Every existing Spec inherits proptest coverage by adding one test."
version      .workspace = true
edition      .workspace = true
rust-version .workspace = true
license      .workspace = true
repository   .workspace = true
homepage     .workspace = true
authors      .workspace = true

[dependencies]
tatara-rust-ast      .workspace = true
tatara-rust-derive   .workspace = true
tatara-rust-validate .workspace = true
proptest             .workspace = true

[lints]
workspace = true
